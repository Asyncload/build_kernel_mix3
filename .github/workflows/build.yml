name: Build Kernel Mix3

on:
  push:
    branches: [ main ]
  workflow_dispatch:
jobs: 
  build_kernel:
    runs-on: ubuntu-22.04
    steps:
      - name: 安装构建依赖项
        run: |
          sudo apt-get update
          sudo apt-get install -y bc bison build-essential curl flex git libncurses5-dev libssl-dev \
                                  make python3 wget zip patch binutils-aarch64-linux-gnu
      - name: 下载内核代码
        uses: actions/checkout@v2
        with:
          repository: LineageOS/android_kernel_xiaomi_sdm845
          ref: lineage-20
          path: kernel

      - name: 修复内核补丁 &
        working-directory: kernel
        run: |
          
          # ===== 2. 您原有的所有修复（已验证可编译） =====
          # 彻底移除无法编译的 elliptic 音频模块
          rm -rf techpack/audio/asoc/elliptic
          rm -rf techpack/audio/asoc/elliptic_common
          sed -i '/elliptic/d' techpack/audio/asoc/Makefile 2>/dev/null || true
          # 注释 halls.c 中调用 elliptic_set_hall_state 的行
          sed -i 's/\(.*elliptic_set_hall_state.*\)/\/\/ \1/' drivers/halls/halls.c
          # 调试模块 trace 路径修复
          sed -i 's|#define TRACE_INCLUDE_PATH .|#define TRACE_INCLUDE_PATH ../../drivers/soc/qcom|' drivers/soc/qcom/tracer_pkt_private.h
          # 修复 USB 驱动双引号问题
          sed -i 's|#include <function/u_ncm.h>|#include "function/u_ncm.h"|' drivers/usb/gadget/configfs.c
          # 修复 idtp9220.c 中的尖括号包含
          sed -i 's/#include <idtp9220.h>/#include "idtp9220.h"/' drivers/power/supply/qcom/idtp9220.c
          # 修复相机传感器模块不在同一目录的路径
          sed -i 's|#include "cam_req_mgr_interface.h"|#include "../cam_req_mgr/cam_req_mgr_interface.h"|' \
            drivers/media/platform/msm/camera/cam_core/cam_context.h
          sed -i 's|#include "cam_cci_dev.h"|#include "../cam_cci/cam_cci_dev.h"|' \
            drivers/media/platform/msm/camera/cam_sensor_module/cam_sensor_io/cam_sensor_i2c.h
          sed -i 's|#include "cam_ife_hw_mgr.h"|#include "../../cam_ife_hw_mgr.h"|' \
            drivers/media/platform/msm/camera/cam_isp/isp_hw_mgr/hw_utils/include/cam_isp_packet_parser.h
          sed -i 's|#include "cam_sensor_cmn_header.h"|#include "../cam_sensor_utils/cam_sensor_cmn_header.h"|' \
            drivers/media/platform/msm/camera/cam_sensor_module/cam_sensor_io/cam_sensor_io.h
          # 批量修复 cam_cci 目录下所有 .c 和 .h 中的尖括号包含
          find drivers/media/platform/msm/camera/cam_sensor_module/cam_cci -type f \( -name "*.c" -o -name "*.h" \) \
            -exec sed -i -E 's|#include <(cam_sensor_[^>]+)>|#include "../cam_sensor_utils/\1"|g' {} \;
          # 修复 cam_sync_util.h 中的尖括号包含
          sed -i 's|#include <cam_sync_api.h>|#include "cam_sync_api.h"|' \
            drivers/media/platform/msm/camera/cam_sync/cam_sync_util.h
          # 修复 cam_sensor_util.h 中的尖括号包含
          sed -i 's|#include <cam_sensor_cmn_header.h>|#include "cam_sensor_cmn_header.h"|' \
            drivers/media/platform/msm/camera/cam_sensor_module/cam_sensor_utils/cam_sensor_util.h
          # 修复相机 cam_trace.h 中的头文件路径
          CAM_TRACE="drivers/media/platform/msm/camera/cam_utils/cam_trace.h"
          sed -i 's|#include "cam_req_mgr_core.h"|#include "../cam_req_mgr/cam_req_mgr_core.h"|' "$CAM_TRACE"
          sed -i 's|#include "cam_req_mgr_interface.h"|#include "../cam_req_mgr/cam_req_mgr_interface.h"|' "$CAM_TRACE"
          sed -i 's|#include "cam_context.h"|#include "../cam_core/cam_context.h"|' "$CAM_TRACE"
          sed -i 's|#define TRACE_INCLUDE_PATH .|#define TRACE_INCLUDE_PATH ../../drivers/media/platform/msm/camera/cam_utils|' "$CAM_TRACE"
          # 蓝牙驱动批量替换尖括号为双引号
          for f in drivers/bluetooth/*.c drivers/bluetooth/*.h; do
            sed -i 's/#include <\(btfm_slim.*\.h\)>/#include "\1"/g' "$f"
          done
          # GPU 驱动修复
          find drivers/gpu/msm -type f \( -name "*.c" -o -name "*.h" \) -exec sed -i 's/#include <kgsl_/#include "kgsl_/g' {} \;
          # 批量修复所有 *_trace.h 中的 TRACE_INCLUDE_PATH（保留您的原有方法）
          for file in $(grep -rl '#define TRACE_INCLUDE_PATH \.$' drivers/ --include='*_trace.h'); do
            rel_path=$(realpath --relative-to=include/trace "$(dirname "$file")" 2>/dev/null || echo "")
            if [ -n "$rel_path" ]; then
              sed -i "s|#define TRACE_INCLUDE_PATH \.$|#define TRACE_INCLUDE_PATH $rel_path|" "$file"
              echo "✅ Fixed: $file -> $rel_path"
            fi
          done

          # 修复 kgsl_events.c 中损坏的包含行
          sed -i 's|#include "kgsl_device\.h>|#include "kgsl_device.h"|' drivers/gpu/msm/kgsl_events.c

      - name: 下载 Clang 13.0.0
        run: |
          wget https://github.com/llvm/llvm-project/releases/download/llvmorg-13.0.0/clang+llvm-13.0.0-x86_64-linux-gnu-ubuntu-20.04.tar.xz
          tar -xf clang+llvm-13.0.0-x86_64-linux-gnu-ubuntu-20.04.tar.xz
          mv clang+llvm-13.0.0-x86_64-linux-gnu-ubuntu-20.04 clang-13
      - name: 配置环境变量 for Clang
        run: |
          export PATH=$PWD/clang-13/bin:$PATH
          export ARCH=arm64
          export CROSS_COMPILE=aarch64-linux-gnu-
          export LLVM=1
          echo "PATH=$PATH" >> $GITHUB_ENV
          echo "ARCH=$ARCH" >> $GITHUB_ENV
          echo "CROSS_COMPILE=$CROSS_COMPILE" >> $GITHUB_ENV
          echo "LLVM=1" >> $GITHUB_ENV

      - name: 配置 kernel
        working-directory: kernel
        run: |
          cp arch/arm64/configs/sdm845_defconfig .config
          cat arch/arm64/configs/vendor/xiaomi/perseus.config >> .config
          scripts/config --disable CONFIG_EFI \
            --disable CONFIG_EFI_STUB \
            --disable CONFIG_EFI_DEV_PATH_PARSER \
            --disable CONFIG_HALL \
            --disable CONFIG_HALL_SENSOR \
            --disable CONFIG_ELLIPTIC \
            --disable CONFIG_ELLIPTIC_SUPPORT \
            --disable CONFIG_MSM_ULTRASOUND \
            --disable CONFIG_SND_SOC_MSM_ULTRA \
            --disable CONFIG_AFE_LOOPBACK
          make ARCH=arm64 olddefconfig
          make scripts
          make prepare

      - name: 构建 kernel
        working-directory: kernel
        run: make -j$(nproc) KCFLAGS="-Wno-error"
        
# >>>>>>>>>>>>>>>>>>>> 关键修改开始 >>>>>>>>>>>>>>>>>>>>
      
      # 1. 下载原厂 boot.img (使用你上传的 Release 链接)
      - name: 下载原厂 boot.img
        run: |
          wget https://github.com/Asyncload/build_kernel_mix3/releases/download/1.0.0/boot.img -O boot.img
          if [ ! -f "boot.img" ]; then
            echo "❌错误：boot.img 下载失败！"
            exit 1
          fi
          echo "✅成功下载原厂 boot.img"

      # 2. 下载 AnyKernel3
      - name: 下载 AnyKernel3 repo
        run: |
          git clone https://github.com/osm0sis/AnyKernel3 -b master
          cd AnyKernel3
          rm -rf .git

      # 3. 准备 AnyKernel3 (修复了 block 路径和 dtbo 设置)
      - name: 准备 AnyKernel3
        run: |
          cd AnyKernel3
          cat > anykernel.sh << 'EOF'
          #!/bin/bash
          # AnyKernel3 Ramdisk Mod Script
          kernel.string=Mix3-KernelSU by Github Action
          do.devicecheck=1
          do.modules=0
          do.systemless=1
          do.cleanup=1
          do.cleanuponabort=0
          device.name1=perseus
          device.name2=mi_mix3
          device.name3=xiaomi
          supported.versions=9 - 14
          patch_vbmeta_flag=auto

          # --- 核心修复 ---
          # 1. 设置正确的 block 路径 (Mix3 是 A/B 分区)
          block=/dev/block/bootdevice/by-name/boot;
          
          # 2. 关闭 dtbo (因为我们只有 boot.img，且内核包含 dtb)
          is_dtbo_device=0;
          
          # 3. 引入核心库
          . tools/ak3-core.sh;
          
          # 4. 执行流程
          split_boot;
          flash_boot;
          EOF

          # 清理旧文件
          rm -f Image* zImage* *dtb* kernel* ramdisk.patch 2>/dev/null || true
          rm -rf ramdisk patch 2>/dev/null || true

      # 4. 复制内核映像 to AnyKernel3
      - name: 复制内核映像 to AnyKernel3
        run: |
          # 确保复制的是 Image.gz-dtb
          if [ -f "kernel/arch/arm64/boot/Image.gz-dtb" ]; then
            cp kernel/arch/arm64/boot/Image.gz-dtb AnyKernel3/
          else
            echo "❌错误：未找到编译产物 Image.gz-dtb！"
            exit 1
          fi
          echo "✅找到到编译产物 Image.gz-dtb！"

      # 5. Create flashable ZIP
      - name: Create flashable ZIP
        run: |
          cd AnyKernel3
          zip -r9 Mix3-$(date +%Y%m%d-%H%M%S).zip .

      # 2. 下载原厂boot.img
      - name: Download Original Boot.img
        run: |
          curl -L https://github.com/Asyncload/build_kernel_mix3/releases/download/1.0.0/boot.img -o original_boot.img
          # 判断：检查文件是否下载成功
          if [ ! -s original_boot.img ]; then echo "❌ 下载失败"; exit 1; fi
          echo "✅ 原厂镜像下载成功"

      # 3. 安装解包/打包工具
      - name: Install Boot.img Tools
        run: |
          sudo apt-get update && sudo apt-get install -y abootimg
          # 判断：检查工具是否可用
          if ! command -v abootimg &> /dev/null; then echo "❌ 工具安装失败"; exit 1; fi
          echo "✅ 工具安装成功"

      # 4. 解包原厂boot.img
      - name: Unpack Original Boot.img
        run: |
          mkdir boot_unpack && cd boot_unpack
          abootimg -x ../original_boot.img
          # 判断：检查是否成功提取出关键文件
          if [ ! -f bootimg.cfg ] || [ ! -f initrd.img ]; then echo "❌ 解包失败"; exit 1; fi
          echo "✅ 原厂镜像解包成功"
      - name: 查找目标文件路径
        run: |
         echo "当前所在目录："
         pwd
         echo "列出所有文件："
         ls -R
         # 如果你想直接搜文件名，可以加这行：
          find . -name "new-boot.img"

      # 5. 替换内核
      - name: Replace Kernel
        run: |
          # 指定你的内核路径
          KERNEL_PATH="kernel/arch/arm64/boot/Image.gz-dtb"
          # 判断：检查该路径下的内核文件是否存在
          if [ ! -f "$KERNEL_PATH" ]; then 
            echo "❌ 错误：找不到内核文件 $KERNEL_PATH，请检查路径！"
            exit 1 
          fi
          
          # 替换操作
          cp "$KERNEL_PATH" boot_unpack/zImage
          echo "✅ 内核替换成功"

      # 6. 生成 new-boot.img
      - name: Repack Boot.img
        run: |
          cd boot_unpack
          abootimg --create ../new-boot.img -f bootimg.cfg -k zImage -r initrd.img
          # 判断：检查新镜像是否生成
          if [ ! -s ../new-boot.img ]; then echo "❌ 重新打包失败"; exit 1; fi
          echo "✅ new-boot.img 生成成功"
          

      # 7. 上传 new-boot.img
      - name: 上传镜像文件boot.img
        uses: softprops/action-gh-release@v2
        with:
          name: New-boot
          tag_name: v_4.9.337
          files: new-boot.img
          
      # 上传内核打包文件
      - name: Upload AnyKernel3 zip
        uses: softprops/action-gh-release@v2
        with:
          name: Mix3-AnyKernel3
          tag_name: v_4.9.337
          files: AnyKernel3/*.zip
