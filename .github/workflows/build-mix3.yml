name: build_Mix3_kernel

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-22.04

    steps:
      - name: 安装构建包
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y --no-install-recommends \
            bc bison build-essential ccache curl flex g++-multilib gcc-multilib \
            git git-lfs gnupg gperf imagemagick protobuf-compiler python3-protobuf \
            lib32readline-dev lib32z1-dev libdw-dev libelf-dev libgnutls28-dev \
            lz4 libsdl1.2-dev libssl-dev libxml2 libxml2-utils lzop pngcrush \
            rsync schedtool squashfs-tools xsltproc zip zlib1g-dev \
            python3 python-is-python3 libtinfo5 libncurses5

      - name: 克隆内核源码
        run: |
          git clone https://github.com/LineageOS/android_kernel_xiaomi_sdm845.git -b lineage-21 --depth=1 kernel-source

      - name: 缓存工具链 (提高二次编译速度)
        id: cache-toolchain
        uses: actions/cache@v4
        with:
          path: |
            clang
            gcc64
            gcc32
          # 只要这里的 key 不变，下次运行就会直接跳过下载
          key: toolchain-sdm845-aosp-v1

      - name: 下载工具链 (仅在无缓存时执行)
        if: steps.cache-toolchain.outputs.cache-hit != 'true'
        run: |
          git clone https://android.googlesource.com/platform/prebuilts/clang/host/linux-x86 --depth=1 clang
          git clone https://android.googlesource.com/platform/prebuilts/gcc/linux-x86/aarch64/aarch64-linux-android-4.9 --depth=1 gcc64
          git clone https://android.googlesource.com/platform/prebuilts/gcc/linux-x86/arm/arm-linux-androideabi-4.9 --depth=1 gcc32

      - name: 执行编译 (暴力掉包 + 缓存加速)
        run: |
          cd kernel-source
          
          # 1. 自动定位工具链 bin 目录
          CLANG_BIN=$(find $GITHUB_WORKSPACE/clang -name "clang" -type f | xargs dirname | head -n 1)
          GCC64_BIN=$(find $GITHUB_WORKSPACE/gcc64 -name "aarch64-linux-android-gcc" -type f | xargs dirname | head -n 1)
          GCC32_BIN=$(find $GITHUB_WORKSPACE/gcc32 -name "arm-linux-androideabi-gcc" -type f | xargs dirname | head -n 1)
          
          # 2. 暴力掉包：创建一个假的 gcc 软链接到 clang
          mkdir -p $GITHUB_WORKSPACE/fake_bin
          ln -sf $CLANG_BIN/clang $GITHUB_WORKSPACE/fake_bin/gcc
          ln -sf $CLANG_BIN/clang $GITHUB_WORKSPACE/fake_bin/aarch64-linux-gnu-gcc
          
          # 3. 设置环境变量
          export PATH=$GITHUB_WORKSPACE/fake_bin:$CLANG_BIN:$GCC64_BIN:$GCC32_BIN:$PATH
          export ARCH=arm64
          export SUBARCH=arm64
          export CLANG_TRIPLE=aarch64-linux-gnu-
          export CROSS_COMPILE=aarch64-linux-android-
          export CROSS_COMPILE_ARM32=arm-linux-androideabi-
          export CC=clang
          
          mkdir -p out
          
          # 4. 生成配置并明确指定编译器
          make O=out ARCH=arm64 CC=clang sdm845_defconfig
          
          # 5. 合并配置
          if [ -f arch/arm64/configs/vendor/xiaomi/perseus.config ]; then
            ARCH=arm64 CC=clang scripts/kconfig/merge_config.sh -O out out/.config arch/arm64/configs/vendor/xiaomi/perseus.config
          fi
          
          # 6. 再次强制刷新配置并绕过检查
          make O=out ARCH=arm64 CC=clang CLANG_TRIPLE=aarch64-linux-gnu- CROSS_COMPILE=aarch64-linux-android- silentoldconfig

          # 7. 开始编译
          make -j$(nproc --all) O=out \
                        ARCH=arm64 \
                        CC=clang \
                        CLANG_TRIPLE=aarch64-linux-gnu- \
                        CROSS_COMPILE=aarch64-linux-android- \
                        CROSS_COMPILE_ARM32=arm-linux-androideabi- \
                        LD=ld.lld \
                        AR=llvm-ar \
                        NM=llvm-nm \
                        OBJCOPY=llvm-objcopy \
                        OBJDUMP=llvm-objdump \
                        STRIP=llvm-strip

      - name: 打包直刷包
        run: |
          git clone https://github.com/osm0sis/AnyKernel3.git
          [ -f kernel-source/out/arch/arm64/boot/Image.gz-dtb ] && cp kernel-source/out/arch/arm64/boot/Image.gz-dtb AnyKernel3/
          [ -f kernel-source/out/arch/arm64/boot/Image.gz ] && cp kernel-source/out/arch/arm64/boot/Image.gz AnyKernel3/
          cd AnyKernel3
          sed -i 's/device.name1=maguro/device.name1=perseus/' anykernel.sh
          zip -r ../Mix3-Kernel.zip *

      - name: 上传
        uses: actions/upload-artifact@v4
        with:
          name: Mix3-Kernel-Result
          path: Mix3-Kernel.zip
